<% ['pete', 'dale'].each do |site| %>
  <% 4.times.each do |i| %>
    topic_<%= i %>_<%= site %>:
      site: <%= site %>
      name: <%= LibLib.sentence(:title) %>
      description: "<%= LibLib.sentences(1..4, :grimm) %>"
  <% end %>
<% end %>

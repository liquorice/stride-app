//
// Buttons
//

@mixin login {
  @include ff_adelle-regular;
  @include breakpoint(huge, overlarge) {
    font-size: 0.75rem;
  }
  @include breakpoint(large) {
    font-size: 0.75rem;
  }
  color: $grey-warm;
  font-size: 13px;
  line-height: 1.4;
  overflow: hidden;
  margin: 0;

  a {
    color: $grey-warm;
    &:active,
    &.active {
      background-color: rgba($grey-warm, 0.08);
    }
  }
}

@mixin button-standard {
  @include ff_adelle-bold;
  @include unselectable;
  @include breakpoint(overlarge,huge) {
    font-size: 1rem;
  }
  @include breakpoint(small,medium,large) {
    font-size: 0.953334rem;
  }
  @include breakpoint(mini) {
    font-size: 15px;
  }
  background: rgba($grey-warm, 0.08);
  border-radius: 3px;
  border: none;
  color: $grey-warm;
  display: inline-block;
  font-size: 1rem;
  line-height: 1;
  padding: 0.571em 0.857em 0.643em;

  &:hover {
    background: rgba($grey-warm, 0.18);
    box-shadow: none;
    color: $black;
    transition: all 100ms ease-out;
  }
}

@mixin button-primary {
  @include button-standard;
  background: $primary;
  color: $white;

  &:hover {
    background: $primary-dark;
    color: $white;
  }
}

@mixin button-short {
  @include button-standard;
  margin-bottom: 0.01rem;
  @include breakpoint(huge) {
    font-size: 0.93rem;
    line-height: 1;
    padding-top: 0.5357em;
  }
  @include breakpoint(overlarge, large) {
    font-size: 0.874rem;
    line-height: 1;
    padding-top: 0.5357em;
  }
  @include breakpoint(medium) {
    font-size: 0.73rem;
    line-height: 1.5;
    padding-top: 0.55em;
  }
  @include breakpoint(mini, small) {
    font-size: 14px;
    line-height: 1.5;
    padding-top: 0.54em;
  }
}

@mixin button-midLevel{
  @include button-standard;
  background: $white;
  color: $links;

  &:hover {
    background: mix($links, white, 20%);
    color: $blue-dark;
  }
}

@mixin button-midLevel-short {
  @include button-short;
  background: $white;
  color: $links;

  &:hover {
    background: mix($links, white, 20%);
    color: $blue-dark;
  }
}

@mixin button-primary-short {
  @include button-short;
  background: $primary;
  color: $white;

  &:hover {
    background: $primary-dark;
    color: $white;
  }
}

@mixin button-meta {
  @include button-short;
  @include p-meta;
  padding: 0.21em 0.75em 0.2877em;
  margin: 0;
}

@mixin button-midLevel-meta {
  @include button-meta;
  background: rgba($links, 0.08);
  color: $links;
  padding: 0.21em 1.357em 0.2877em;

  &:hover {
    background: mix($links, white, 20%);
    color: $blue-dark;
  }
}
// TODO These are debug styles only

.message {
  border: 1px solid black;
  margin: 10px;
  padding: 10px;

  p {
    display: inline-block;
  }
}

  .avatarWrapper {
    width: 20px;
    height: 20px;
    float: left;

    .message[data-self=true] & {
      float: right;
    }
  }

.messages {
  bottom: 0;
  position: absolute;
  width: 100%;
  min-height: 300px;
}

.message_window {
  height: 300px;
  overflow-y: scroll;
  position: relative;
}